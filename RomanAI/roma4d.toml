# RomanAI package root — resolves roma4d.libgeo → roma4d/libgeo.r4d
name = "romanai"
version = "0.1.0"
edition = "2025"

[package]
authors = ["RomanAI Labs"]
description = "4D-native GGUF runner (RomanAI)."

[build]
default_backend = "llvm"
backend = "llvm"
incremental = true

[systems]
gc = false
unsafe = true
